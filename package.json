{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Vanilla HTML/CSS/JS Portfolio",
  "private": true,
  "homepage": "https://shre2201.github.io/portfolio",
  "scripts": {
    "start": "npx serve .",
    "predeploy": "powershell -Command \"New-Item -ItemType Directory -Force -Path dist; Copy-Item *.html, styles.css, script.js, bg.png, .nojekyll dist\\ -Force\"",
    "deploy": "npx gh-pages -d dist --nojekyll"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}